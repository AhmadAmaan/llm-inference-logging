apiVersion: v1
kind: Service
metadata:
  name: llm-inference-app
  namespace: llm-inference-logging
spec:
  selector:
    app: llm-inference-app
  ports:
    - name: http
      port: 80
      targetPort: 3000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: llm-inference-app
  namespace: llm-inference-logging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: llm-inference-app
  template:
    metadata:
      labels:
        app: llm-inference-app
    spec:
      containers:
        - name: web
          image: llm-inference-logging:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: llm-inference-app-config
            - secretRef:
                name: llm-inference-app-secrets
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 1Gi
