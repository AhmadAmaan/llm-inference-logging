apiVersion: v1
kind: Service
metadata:
  name: {{ include "llm-inference-logging.fullname" . }}
  labels:
    {{- include "llm-inference-logging.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "llm-inference-logging.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
